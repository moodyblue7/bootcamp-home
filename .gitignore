# ── 의존성 / 빌드 산출물 ──
/node_modules
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# ── 환경변수 / 배포 ──
.env
.env*.local
.vercel

# ── 내부 전용 자료 (외부 공개 금지: 대외비·초상권·매출·사실원장) ──
# 사이트 빌드에는 필요 없으며, 로컬에만 보관한다.
/ref/
/assets/
/docs/
/CLAUDE.md
/scripts/

# ── 기타 ──
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
